import Loading from "@/components/client-components/Loading";
import React from "react";

const loading = () => {
  return (
    <div className="w-full">
      <Loading />
    </div>
  );
};

export default loading;
